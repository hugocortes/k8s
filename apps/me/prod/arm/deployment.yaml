kind: Deployment
apiVersion: extensions/v1beta1
metadata:
  name: me
spec:
  template:
    metadata:
      name: me
    spec:
      nodeSelector:
        beta.kubernetes.io/arch: arm
      containers:
      - name: me
        imagePullPolicy: Always
