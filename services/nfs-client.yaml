# nfs client custom settings
replicaCount: 2
nfs:
  server: server.home.lan
  path: /mnt/hdd/k8s
  mountOptions:
  - tcp
  - nfsvers=4
resources:
  limits:
   cpu: 100m
   memory: 128Mi
  requests:
   cpu: 100m
   memory: 128Mi
storageClass:
  defaultClass: true
