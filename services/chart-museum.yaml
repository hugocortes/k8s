# chart museum custom settings
env:
  open:
    ALLOW_OVERWRITE: true
    DISABLE_API: false
    DISABLE_METRICS: false
    STORAGE: google
    STORAGE_GOOGLE_PREFIX: charts
persistence:
  enabled: true
gcp:
  secret:
    enabled: true
