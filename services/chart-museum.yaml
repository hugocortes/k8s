---

env:
  open:
    ALLOW_OVERWRITE: true
    DISABLE_API: false
    DISABLE_METRICS: false
    STORAGE: google
    STORAGE_GOOGLE_PREFIX: charts
persistence:
  enabled: true
gcp:
  secret:
    enabled: true
resources:
 requests:
   cpu: 125m
   memory: 128Mi
 limits:
   cpu: 250m
   memory: 256Mi
