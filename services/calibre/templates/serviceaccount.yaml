{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "calibre.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "calibre.labels" . | nindent 4 }}
{{- end -}}
