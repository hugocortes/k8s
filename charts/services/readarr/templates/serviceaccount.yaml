{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "readarr.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
{{ include "readarr.labels" . | nindent 4 }}
{{- end -}}
