apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ombi.fullname" . }}-test-connection"
  labels:
{{ include "ombi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ombi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
