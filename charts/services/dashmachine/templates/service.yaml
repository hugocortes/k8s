apiVersion: v1
kind: Service
metadata:
  name: {{ include "dashmachine.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "dashmachine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dashmachine.selectorLabels" . | nindent 4 }}
